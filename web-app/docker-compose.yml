version: '3.4'

services:
  # Nginx
  sw-analytics-tool:
    image: "nginx:1.17.4"
    container_name: sw-analytics-tool
    restart: always
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./app:/usr/share/nginx/html
    ports:
      - 8080:80